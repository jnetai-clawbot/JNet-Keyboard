
Project Location /home/jay/Documents/Scripts/AI/OpenCode/Android-Keyboard/

I love the new layout on version 1.0.3
all version s above 1.0.3 should be removed from github repo as they are not working correctly

now for 1.0.4 i need the following
can you make 2nd row of letters (2nd row from the bottom) centered to match the rest.

Add more unicode fonts under the current font list (add all the missing as iv noticed at least 1 font is missing, called strikethrough (aka strikethrough/overlay text for example) and more I may of forgotten but should be unicode and i want all the unicode fonts.

I love the green light on shift button keep the look of the buttons exactly how it is.
The enter key icon inside the button needs to be bigger (same size as the settings cog is for example).
the enter button should send like im pressing the enter button not a new line as it currently is

The check latest update (on github using api) button on the app (in About section) gives error fails to check for latest, maybe just point that button to github latest link 
release.

release this update using the same keystore and build in github using actions / workflows as version 1.0.4


